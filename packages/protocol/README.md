# @clover.js/protocol

Clover 的最小协议核心。

当前状态：

- 首批稳定消费面
- 适合作为外部项目最先接入的 Clover 包

首批内容：

- `None`
- `CloverError<Code, Payload>`
- `Option` / `Result`
- `Brand`
- 数值品牌类型
- `isNone` / `isError` / `isErrorPayload`
- `createError`
- `assertNever`
